# Archive - Pre-Redesign Examples

> Visual artefacts produced before the May 2026 website redesign.
> **Retained for historical reference only. Not canonical visual direction.**
> If you're calibrating against Enablement's current brand, look in [`../inspiration/`](../inspiration/), not here.

## What's in this folder

### LinkedIn personal banners (1584x396)

Four production variants currently live on Lanny's LinkedIn profile.

| File | Background | Photo |
|---|---|---|
| `banner-01_black-lanny.png` | Dark (near-black + subtle red gradient bleed) | Lanny right |
| `banner-02_black-neutral.png` | Same dark canvas | None |
| `banner-03_red-neutral.png` | Saturated Enablement Red | None |
| `banner-04_red-lanny.png` | Saturated Enablement Red | Lanny right |

All four share the same composition: Enablement logo top-left · "GTM Agents that create Sales Pipeline" headline centered-right · three pillar pills below · trust strip with 5 partner logos at the bottom.

### Why these are pre-redesign

- Predate the website's current `--color-canvas` `#F2F4F8` light slate canvas.
- Use saturated red full-bleed, which conflicts with the new "subtle gradient + grid texture" background language.
- No glassmorphic surface treatment on pills (flat-fill).
- Pillar names are inconsistent across the four variants ("Content / AI Always / Outreach" vs "Content / Outbound / AI RevOps"), suggesting positioning was still being iterated on.

### Lanny's assessment

"Okay, but not completely right. They should be redesigned to reflect the website redesign."

### Rebuild brief (parked)

When the designer is ready to refresh the banner set, the brief should apply:

- Gradient background (`--color-canvas` top-left → `--color-flow` 8% bottom-right)
- Grid texture overlay (~24px dot grid, `--color-border` at 20% opacity)
- Glassmorphic pillar pills (subtle gradient fill, 1px border, soft shadow)
- Sofia Sans display headline (test: lock the pillar names first, then write the headline)
- Bottom trust strip (existing pattern works)
- Optional photo treatment, refined for the new background

Banner rebuild should happen after the first cheat-sheet trial validates the new visual register, not before.

## Do not use these as visual reference for new work

Designer onboarding flow points at [`../inspiration/`](../inspiration/) for canonical references. This folder exists so the rebuild brief has access to what's currently shipped, nothing more.
